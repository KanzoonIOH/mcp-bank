services:
    mcp-bank:
        build: .
        ports:
            - "7777:8000"
        environment:
            FASTMCP_TRANSPORT: streamable-http
            FASTMCP_HOST: 0.0.0.0
            FASTMCP_PORT: 8000
        restart: unless-stopped
        networks:
            - shared-net
networks:
    shared-net:
        external: true
